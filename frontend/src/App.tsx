// CloudLab Platform — App root
// Phase 0.2: skeleton. Full routing and layout in Phase 3.

function App() {
  return (
    <div className="min-h-screen bg-gray-50 flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-4xl font-bold text-brand-900">☁️ CloudLab</h1>
        <p className="mt-2 text-gray-500">Network labs, ready to use.</p>
        <p className="mt-1 text-sm text-gray-400">
          Phase 0.2 — dev environment running
        </p>
      </div>
    </div>
  );
}

export default App;
